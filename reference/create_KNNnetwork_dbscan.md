# create_KNNnetwork_dbscan

Create a spatial knn network with dbscan

## Usage

``` r
create_KNNnetwork_dbscan(
  spatial_locations,
  sdimx = "sdimx",
  sdimy = "sdimy",
  sdimz = "sdimz",
  k = 4,
  ...
)
```

## Value

spatial knn network with dbscan
