# Spatial Value Key

A metaprogramming object that references a set of information to get
from a `giotto` object when used as `svkey@get(gobject)`. Referenced
data will be retrieved as a `data.table` via
[`spatValues()`](https://giotto-suite.github.io/GiottoClass/reference/spatValues.md)
