# .create_delaunaynetwork_deldir

Create a spatial Delaunay network with deldir

## Usage

``` r
.create_delaunaynetwork_deldir(
  spatial_locations,
  sdimx = "sdimx",
  sdimy = "sdimy",
  ...
)
```

## Value

spatial Delaunay network with deldir
