# Overlap points per polygon – wrapped

overlap wrapped polygons

## Usage

``` r
.overlap_points_per_polygon_wrapped(
  spatvec_wrapped,
  pointvec_wrapped,
  poly_ID_names
)
```

## Value

Packed object
