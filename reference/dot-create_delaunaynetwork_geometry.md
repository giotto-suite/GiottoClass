# .create_delaunaynetwork_geometry

Create a spatial Delaunay network.

## Usage

``` r
.create_delaunaynetwork_geometry(
  spatial_locations,
  sdimx = "sdimx",
  sdimy = "sdimy",
  options = "Pp",
  ...
)
```

## Value

spatial Delaunay network
