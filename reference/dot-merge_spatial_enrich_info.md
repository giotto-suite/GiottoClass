# .merge_spatial_enrich_info

take a combine data.table result and append spatial enrichment results.

## Usage

``` r
.merge_spatial_enrich_info(
  gobject,
  comb_dt,
  spat_unit,
  feat_type,
  spat_enr_names = NULL
)
```

## Value

data.table
