% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_structures.R
\name{createSpatialKNNnetwork}
\alias{createSpatialKNNnetwork}
\title{createSpatialKNNnetwork}
\usage{
createSpatialKNNnetwork(
  gobject,
  method = "dbscan",
  spat_unit = NULL,
  feat_type = NULL,
  spat_loc_name = NULL,
  dimensions = "all",
  name = "knn_network",
  k = 4,
  maximum_distance = NULL,
  minimum_k = 0,
  verbose = FALSE,
  return_gobject = TRUE,
  output = c("spatialNetworkObj", "data.table"),
  space = NULL,
  ...
)
}
\arguments{
\item{gobject}{giotto object}

\item{method}{method to create kNN network}

\item{spat_unit}{spatial unit}

\item{feat_type}{feature type}

\item{spat_loc_name}{name of spatial locations}

\item{dimensions}{which spatial dimensions to use (default = all)}

\item{name}{name for spatial network (default = 'spatial_network')}

\item{k}{number of nearest neighbors based on physical distance}

\item{maximum_distance}{distance cuttof for nearest neighbors to consider
for kNN network}

\item{minimum_k}{minimum nearest neigbhours if maximum_distance != NULL}

\item{verbose}{verbose}

\item{return_gobject}{boolean: return giotto object (default = TRUE)}

\item{output}{character. Object type to return spatial network as when
\code{return_gobject = FALSE}. (default: 'spatialNetworkObj')}

\item{space}{(\code{giottoMulti} only) \code{character(1)}. Name of a coordinate
frame recorded on the object, or \code{NULL} (default) for each sample's own
native frame. The frame decides the job: every sample it covers gets a
network built in that frame's coordinates, written into its own
\verb{@spatial_network} slot — so this mutates the wrapped children.

This is \strong{not} a sample selector. An artifact generator takes none,
because once the rows are in a slot nothing downstream can tell which
the selector admitted (see \code{adr/0006}). To build over a subset of
samples, record a space over them, or subset with \code{mg[...]} first.}

\item{\dots}{additional arguments to the selected method function}
}
\value{
giotto object with updated spatial network slot

\strong{dimensions: } default = 'all' which takes all possible dimensions.
Alternatively you can provide a character vector that specififies the
spatial dimensions to use, e.g. c("sdimx', "sdimy")
or a numerical vector, e.g. 2:3

\strong{maximum_distance: } this is a post-filter on the k neighbours the
search already found, not a constraint on the search itself. To build a
network on distance alone, prefer \code{\link[=radiusNetworkParam]{radiusNetworkParam()}}, which searches
only within the radius; the older advice of setting \code{k} very high (e.g.
\code{k = 100}) and filtering still works, but it finds a hundred neighbours per
cell in order to discard most of them.
}
\description{
Create a spatial knn network.
}
\examples{
g <- GiottoData::loadGiottoMini("visium")

createSpatialKNNnetwork(g)

}
