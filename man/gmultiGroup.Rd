% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmulti.R
\name{gmultiGroup}
\alias{gmultiGroup}
\alias{gmultiGroup,giottoMulti-method}
\alias{gmultiGroup<-}
\alias{gmultiGroup<-,giottoMulti-method}
\alias{gmultiGroups}
\alias{gmultiGroups,giottoMulti-method}
\title{gmulti sample group accessor}
\usage{
gmultiGroup(x, group, ...)

\S4method{gmultiGroup}{giottoMulti}(x, group, ...)

gmultiGroup(x, group, ...) <- value

\S4method{gmultiGroup}{giottoMulti}(x, group, ...) <- value

gmultiGroups(x, ...)

\S4method{gmultiGroups}{giottoMulti}(x, ...)
}
\arguments{
\item{x}{a \code{giottoMulti}}

\item{group}{\code{character(1)}. Group name.}

\item{...}{additional arguments, currently unused}

\item{value}{\code{character} of member names (samples or other groups), or
\code{NULL} to drop the group. May be named, in which case the \strong{names}
are the members and the values are per-child content handles, matching
the shape of a \verb{@mapping} entry.}
}
\value{
\code{gmultiGroup()} the member vector, or \code{NULL} if unregistered;
\code{gmultiGroups()} the registered group names
}
\description{
Get or set the \verb{@groups} slot: registers a name that refers to several
samples at once. A group name is usable \strong{anywhere a sample name is} —
\verb{samples =}, \verb{object =}, every getter — so registering one adds no new
parameter to any signature.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{gmultiGroup(mg, "tumor_pair") <- c("B191", "B215")
getCellMetadata(mg, samples = "tumor_pair")
spatPlot2D(mg, samples = "tumor_pair")
}\if{html}{\out{</div>}}

Groups may name other groups; membership is resolved recursively at use
time, deduplicated with \code{\link[=unique]{unique()}} in first-appearance order, so
overlapping groups never read a child twice. Assigning \code{NULL} drops a
group.
}
\section{Late binding}{

A group stores the names it was given, not the samples they resolved to
at the time. Membership is expanded when the group is \emph{used}, so a group
keeps tracking the child population as it changes. The same reason
filter predicates resolve against current metadata rather than against
a snapshot.

The consequence is that a group naming a child that has since been
removed is an error at use time, not at removal time — \verb{@groups} is
deliberately not pruned by \code{[}. Renaming a child \emph{does} rewrite group
members, because a rename has one correct rewrite and a removal has
none.
}

\section{Collisions}{

A group and a child may not share a name, and the clash is rejected at
registration from whichever side arrives second — \verb{gmultiGroup<-} refuses
a name already held by a child, and \verb{[[<-} / \verb{names<-} refuse a child
name already held by a group. Resolution therefore never has to break a
tie, which is the point: a tie-break rule would make the answer depend
on registration order.
}

\examples{
\dontrun{
gmultiGroup(mg, "tumor_pair") <- c("B191", "B215")
gmultiGroup(mg, "all_tumor") <- c("tumor_pair", "B651")  # nested
gmultiGroups(mg)
gmultiGroup(mg, "tumor_pair") <- NULL
}
}
