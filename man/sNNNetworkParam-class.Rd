% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/NN_network.R
\docType{class}
\name{sNNNetworkParam-class}
\alias{sNNNetworkParam-class}
\alias{sNNNetworkParam}
\title{sNNNetworkParam — Shared-Nearest-Neighbour Network Param}
\usage{
sNNNetworkParam(
  k = 30L,
  top_shared = 3L,
  minimum_shared = 5L,
  weight_fun = function(d) 1/(1 + d),
  include_weight = TRUE,
  include_distance = TRUE,
  output = c("auto", "data.table", "igraph", "parquet"),
  engine = c("dbscan", "hnsw"),
  ef = 200,
  n_threads_build = 1L
)
}
\arguments{
\item{k}{number of neighbours used to compute sharing}

\item{top_shared}{keep at least this many edges per node}

\item{minimum_shared}{keep edges with at least this many shared neighbours}

\item{weight_fun}{function mapping distance to weight}

\item{include_weight, include_distance}{include columns in output}

\item{output}{one of \code{"auto"}, \code{"data.table"}, \code{"igraph"}, \code{"parquet"}}

\item{engine}{character. kNN search backend. \code{"dbscan"} (default) is exact
and single-threaded, and is the better choice on small data where it is
also faster. \code{"hnsw"} uses an approximate HNSW index via \code{\link[=hnswKNN]{hnswKNN()}} --
multithreaded and free of the kd-tree's degradation at PCA
dimensionality, which is what makes it worth reaching for on large
datasets. On 158,662 cells at \code{k = 30} it reproduced 99.995\% of the exact
network's undirected edges in 11.8s against 79.9s. Requires
\pkg{RcppHNSW}. An \code{"auto"} setting that picks by dataset size is planned.}

\item{ef}{integer. \code{"hnsw"} only, ignored otherwise. Search beam width,
default 200 -- the recall/speed dial. Higher values search more of the
graph, moving the result closer to the exact \code{"dbscan"} answer at the cost
of query time.}

\item{n_threads_build}{integer or \code{NULL}. \code{"hnsw"} only, ignored otherwise.
Threads for the index build, default \code{1}. A parallel build is not
reproducible -- insertion order varies, so neighbours differ slightly
between runs and that propagates to clustering even with a fixed seed.
\code{NULL} inherits the search thread count and trades reproducibility for
speed.}
}
\description{
Constructor and class for shared-Nearest-Neighbour network parameters.
sNN edges are symmetric by definition (\verb{|N(a) ∩ N(b)| = |N(b) ∩ N(a)|}),
so the resulting graph is \strong{undirected} — one edge per pair.
}
\section{Slots}{

\describe{
\item{\code{k}}{integer. number of nearest neighbours used to compute sharing.}

\item{\code{top_shared}}{integer. keep at least this many edges per node.}

\item{\code{minimum_shared}}{integer. keep edges with at least this many shared
neighbours.}

\item{\code{weight_fun}}{function. weight = \code{weight_fun(distance)}.}

\item{\code{include_weight,include_distance}}{logical. include columns.}

\item{\code{output}}{character. See \code{\link[=createNetwork]{createNetwork()}}.}
}}

