% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nn_search_hnsw.R
\name{nnToUwot}
\alias{nnToUwot}
\title{Convert a kNN object to uwot's precomputed-neighbor format}
\usage{
nnToUwot(nn)
}
\arguments{
\item{nn}{object of class \code{kNN}/\code{NN}, or a list with \code{id} and \code{dist}
matrices of equal dimensions.}
}
\value{
list with \code{idx} (integer matrix, \code{n} x \code{k + 1}) and \code{dist}
(numeric matrix, same shape), suitable as uwot's \code{nn_method}.
}
\description{
Reshape a \code{c("kNN", "NN")} object -- from \code{\link[=hnswKNN]{hnswKNN()}} or \code{\link[dbscan:kNN]{dbscan::kNN()}} --
into the \code{list(idx =, dist =)} that \code{\link[uwot:umap]{uwot::umap()}} and \code{\link[uwot:umap2]{uwot::umap2()}}
accept as \code{nn_method}, so a graph built once can be handed to UMAP instead
of letting uwot run a second, independent search.
}
\details{
Two differences have to be reconciled, and uwot validates neither, so
getting either wrong corrupts the embedding silently rather than raising an
error:
\itemize{
\item uwot names the neighbor matrix \code{idx}; \code{kNN} objects name it \code{id}.
\item uwot requires each observation to be its own first neighbor
(\code{idx[, 1] == seq_len(n)}, \code{dist[, 1] == 0}), because it drops column 1
when fitting the local-connectivity offset. Both \code{\link[=hnswKNN]{hnswKNN()}} and
\code{\link[dbscan:kNN]{dbscan::kNN()}} \emph{remove} self-matches. Passing their output unchanged
therefore discards every observation's true nearest neighbor and fits
\code{rho} against a shifted distance set.
}

The returned matrices are \code{k + 1} columns wide: the self column plus the
\code{k} neighbors. uwot ignores \code{n_neighbors} when given a graph, so that width
is what sets the neighborhood size.
}
\examples{
\dontrun{
m <- matrix(rnorm(1000 * 20), nrow = 1000)
nn <- hnswKNN(m, k = 29)
uwot::umap2(m, nn_method = nnToUwot(nn))
}
}
