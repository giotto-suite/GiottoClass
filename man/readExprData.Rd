% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_input.R
\name{readExprData}
\alias{readExprData}
\title{Read expression data}
\usage{
readExprData(
  data_list,
  sparse = TRUE,
  cores = determine_cores(),
  default_feat_type = NULL,
  verbose = TRUE,
  provenance = NULL,
  expression_matrix_class = deprecated()
)
}
\arguments{
\item{data_list}{nested \code{list} of expression input data}

\item{sparse}{(\code{logical}, default = TRUE) read matrix data in a sparse manner}

\item{cores}{number of cores to use}

\item{default_feat_type}{(optional) default feat_type to use}

\item{verbose}{be verbose}

\item{provenance}{(optional) provenance information}

\item{expression_matrix_class}{deprecated. See \code{?createExprObj} for details}
}
\value{
exprObj
}
\description{
Read a nested list of expression data inputs in order to
generate a list of giotto-native exprObj that are addressed to specific
spat_unit and feat_type based on list naming and defaults.
}
\examples{
mylistA = list('a' = matrix(seq(5)), 'b' = matrix(seq(5)))
GiottoUtils::depth(mylistA)

mylistB = list(
    A = list('a' = matrix(seq(5)), 'b' = matrix(seq(5))),
    B = list('c' = matrix(seq(5)),'d' = matrix(seq(5)))
)
GiottoUtils::depth(mylistB)

mylistC = list(
    'RNA' = list(
        'RAW' = list('cell' = matrix(seq(5)),
        'nucleus' = matrix(seq(6,10))),
        'NORM' = list('cell' = matrix(seq(11,15)),
        'nucleus' = matrix(seq(20,25)))
    ),
    'PROT' = list(
        'RAW' = list('cell' = matrix(seq(16,20)))
    )
)
GiottoUtils::depth(mylistC)

readExprData(mylistA)
readExprData(mylistB)
readExprData(mylistC)
}
