% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gmulti.R
\docType{class}
\name{giottoMulti-class}
\alias{giottoMulti-class}
\alias{giottoMulti}
\title{S4 giottoMulti}
\value{
giottoMulti object
}
\description{
Container for multiple \code{giotto} objects whose spatial information is kept
separate (one space per child) but whose expression-space analysis is
shared across all cells.
}
\section{Slots}{

\describe{
\item{\code{objects}}{named \code{list} of \code{giotto} objects (children)}

\item{\code{id_map}}{\code{list} with elements \code{cells} and \code{feats}, each a \code{data.table}
mapping \verb{(object, local_id) -> global_id}. This is an identity
\strong{registry} covering every child ID, and is never narrowed.}

\item{\code{id_sig}}{\code{list} of per-child ID length-signatures. Drives id_map cache
invalidation in \code{initialize()}.}

\item{\code{mapping}}{\code{list} declaring how child-level content federates up to
gmulti-level handles. Three named entries — \code{spat_unit}, \code{feat_type},
and \code{values} (expression matrix names; flat, not per-universe) — each a
list of named character vectors mapping \verb{sample -> child-level name}.
Every entry keys every sample; \code{NA_character_} is the deliberate-skip
sentinel ("this sample does not contribute to this handle").
Auto-discovered at construction; edited via \code{\link[=gmultiMapping]{gmultiMapping()}}.}

\item{\code{groups}}{named \code{list} of character vectors declaring that a name
refers to several samples at once. Usable anywhere a sample name is,
so no \verb{samples =} formal changes. Entries may name other groups
(resolved recursively); membership is expanded at \strong{resolution}, not
at registration, so a group tracks the current child population.
Edited via \code{\link[=gmultiGroup]{gmultiGroup()}}. Unlike every other registry here,
\verb{@groups} is \strong{not} reset when the child population changes — see
the note in \code{initialize()}.}

\item{\code{expression}}{shared expression matrices (rows = union of features,
cols = global cell IDs)}

\item{\code{expression_feat}}{available feature types}

\item{\code{cell_metadata}}{shared cell metadata (one row per global cell ID)}

\item{\code{feat_metadata}}{shared feature metadata (one row per global feature)}

\item{\code{cell_ID}}{shared cell ID lists (global IDs). Records active narrowing
from stage 2; \code{NULL} means unfiltered.}

\item{\code{feat_ID}}{shared feature ID lists (global IDs), same contract.}

\item{\code{dimension_reduction}}{shared joint dim-reductions (PCA, UMAP, harmony)}

\item{\code{nn_network}}{shared joint NN graphs}

\item{\code{spatial_locs}}{\strong{multi-level} spatial locations, nested
\code{spat_unit -> name}. Cell IDs are \code{sample::id} globals.}

\item{\code{spatial_info}}{\strong{multi-level} polygons, keyed \code{name}. \code{poly_ID}s are
\code{sample::id} globals.}

\item{\code{feat_info}}{\strong{multi-level} points, keyed \code{feat_type}. Features are
never sample-namespaced, so these carry plain \code{feat_ID}s.}

\item{\code{images}}{\strong{multi-level} images, keyed \code{name} — one raster covering
several samples, as a combined space produces.}

\item{\code{spatial_network}}{\strong{cross-sample} spatial networks, nested
\code{spat_unit -> name} — the same shape as \code{giotto@spatial_network}.
Both endpoints of every edge are \code{sample::id} globals, so an edge may
span samples, which is why these cannot live on a child whose slot
knows only its own cell IDs. Per-sample networks are \emph{not} here; they
stay on the children.

A network built in a coordinate frame takes that frame's name by
default, so two frames do not collide, and carries the frame in its
\verb{@parameters$space}. See \code{adr/0006}.

These five spatial slots exist for the same reason: a combined analysis
produces \strong{one} artifact spanning the federation, and there is no child
that can hold it. They are also what makes the round trip land somewhere
— read polygons across children, buffer them, write the result back, and
the result is multi-level because it no longer belongs to any one sample.
Setters on a \code{giottoMulti} write here and nowhere else (see the note on
the spatial setters below).}

\item{\code{spatial_enrichment}}{shared spatial enrichment results}

\item{\code{multiomics}}{shared multi-omics info}

\item{\code{instructions}}{giotto-style instructions}

\item{\code{parameters}}{analysis parameters (mirrors \code{giotto@parameters})}

\item{\code{versions}}{package versions}

\item{\code{misc}}{miscellaneous}

\item{\code{source}}{on-disk source / project manager (e.g.
\link[GiottoDisk:gDirSource]{GiottoDisk::gDirSource}) where multi-level shared-domain artifacts
live. \code{NULL} for in-memory multis. Children may carry their own
per-sample sources; multi-level slots (shared \verb{@expression},
\verb{@dimension_reduction}, \verb{@nn_network}) write to this one.}

\item{\code{view}}{named \code{list} of view recipes. Reserved for stage 4.}

\item{\code{spaces}}{named \code{list} of coordinate-frame recipes. Reserved for stage 4.}
}}

