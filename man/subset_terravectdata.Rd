% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-extract.R
\name{subset_terravectdata}
\alias{subset_terravectdata}
\alias{subset,giottoPoints-method}
\title{Subset terraVectData subobjects}
\usage{
\S4method{subset}{giottoPolygon}(x, cell_ids = NULL, feat_ids = NULL, feat_type = NULL, ...)

\S4method{subset}{giottoPoints}(x, feat_ids = NULL, negate = FALSE, ...)
}
\arguments{
\item{x}{a \code{giottoPolygon} or \code{giottoPoints} object}

\item{cell_ids}{character. Polygon IDs to keep (for \code{giottoPolygon})}

\item{feat_ids}{character. Feature IDs to keep}

\item{feat_type}{character. Feature type(s) to subset overlaps within.
Use \code{":all:"} for all feature types.}

\item{\dots}{not used}
}
\value{
object of same class as \code{x}, subsetted
}
\description{
Subset \code{giottoPolygon} and \code{giottoPoints} by cell or feature IDs.
All relevant slots (geometries, centroids, overlaps) are subsetted
consistently. Intended as a stable integration point for \code{subsetGiotto}
and extending packages — the \code{[} methods remain the low-level workhorse
for logical/numeric indexing.
}
\examples{
gpoly <- GiottoData::loadSubObjectMini("giottoPolygon")
subset(gpoly, cell_ids = spatIDs(gpoly)[1:10])
subset(gpoly, cell_ids = spatIDs(gpoly)[1:10], negate = TRUE)

gpoints <- GiottoData::loadSubObjectMini("giottoPoints")
subset(gpoints, feat_ids = "Adgrl1")
}
