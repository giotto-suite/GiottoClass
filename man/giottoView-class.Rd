% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-view.R
\docType{class}
\name{giottoView-class}
\alias{giottoView-class}
\title{Class for subset / narrowing recipes}
\value{
a \code{giottoView} object
}
\description{
A \code{giottoView} is a deferred, read-only NARROWING of a \link[=giotto-class]{giotto} or
\link[=giottoMulti-class]{giottoMulti} object: a recipe, re-resolved against the object's
current state each time it is consumed, rather than a snapshot.

Access it with \code{[} (class-preserving, so the result stays editable) and
\code{[[} (extracts a step). Append to it with the builder verbs \code{\link[=subset]{subset()}}
(a filter step, or a sample step with \verb{samples =}) and \code{\link[=crop]{crop()}}, or
compose two with \code{+}. Export the
plain nested form with \code{\link[=as.list]{as.list()}}.
}
\section{Slots}{

\describe{
\item{\code{steps}}{\code{list} of recorded steps, in order. Each step is a tagged
plain list -- \code{list(type = "filter" | "crop" | "samples", ...)} -- and
carries no closure and no external pointer, so a recipe survives
\code{saveRDS()} and reaches a parallel worker. A \code{crop} step also carries
the \code{space} its region was drawn in.}
}}

\examples{
g <- crop(giotto(), c(0, 10, 0, 10), view = "v")
v <- giottoView(g, "v")
v[[1L]]
as.list(v)
}
\seealso{
\code{\link[=giottoView]{giottoView()}} for the gobject-level accessors;
\link[=giottoSpace-class]{giottoSpace} for the coordinate-frame recipe
}
