% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-area.R
\name{expanse}
\alias{expanse}
\alias{expanse,giottoPolygon-method}
\alias{area,giottoPolygon-method}
\title{Get the area of individual polygons}
\usage{
\S4method{expanse}{giottoPolygon}(x, output = c("data.table", "named", "vector"), ...)

\S4method{area}{giottoPolygon}(x, ...)
}
\arguments{
\item{x}{\code{giottoPolygon}}

\item{output}{one of \code{"data.table"} (default), \code{"named"}, or \code{"vector"}.
\code{"data.table"} returns a \code{data.table} with columns \code{cell_ID} and \code{area}.
\code{"named"} returns a named numeric vector with \code{cell_ID} as names.
\code{"vector"} returns a plain unnamed numeric vector (terra-compatible).}

\item{...}{
  Arguments passed on to \code{\link[terra:expanse]{terra::expanse}}
  \describe{
    \item{\code{}}{}
  }}
}
\value{
depends on \code{output}: a \code{data.table}, named \code{numeric}, or \code{numeric}
}
\description{
Compute the area covered by polygons
}
\details{
Giotto's \code{expanse()} method dispatches on terra's generic. When the
underlying data is a \code{SpatVector}, \code{transform} defaults to \code{FALSE} and
CRS warnings are suppressed — both appropriate for biological coordinate
systems. When the underlying data is a non-terra representation (e.g. a
disk-backed store), the call is forwarded to a method for that class.
}
\examples{
sl <- GiottoData::loadSubObjectMini("spatLocsObj")
gpoly <- GiottoData::loadSubObjectMini("giottoPolygon")
gpoints <- GiottoData::loadSubObjectMini("giottoPoints")

# area of polygons
head(expanse(gpoly))

# area of the convex hull
expanse(hull(sl))
feature_hulls <- hull(gpoints, by = "feat_ID")
head(expanse(feature_hulls))

# output formats
expanse(gpoly, output = "named")
expanse(gpoly, output = "vector")

}
