% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods-recipe.R
\name{spaceSamples}
\alias{spaceSamples}
\alias{spaceSamples,combinedSpace-method}
\alias{spaceSamples,perSampleSpace-method}
\title{Which samples a coordinate frame spans}
\usage{
spaceSamples(x, ...)

\S4method{spaceSamples}{combinedSpace}(x, ...)

\S4method{spaceSamples}{perSampleSpace}(x, ...)
}
\arguments{
\item{x}{a \link[=giottoSpace-class]{giottoSpace}}

\item{...}{additional arguments, currently unused}
}
\value{
\code{character} of sample names, or \code{NA_character_}
}
\description{
The membership a frame declares — the thing an artifact generator reads
to size its job (\code{adr/0006}).
\itemize{
\item \link[=combinedSpace-class]{combinedSpace} — the member names, in recording order.
\item \link[=perSampleSpace-class]{perSampleSpace} — \code{NA_character_}, meaning "no sample identity":
the frame applies to each sample independently, so the job size comes
from the object rather than from the frame.
}

\code{NA_character_} rather than \code{character(0)}: an empty vector reads as
"nobody participates", which is a real and different answer (a
\code{combinedSpace} that has been emptied). A caller that branches on
\code{length()} alone would run zero jobs for the frame that should run one
per sample.
}
\examples{
g <- spatShift(giotto(), dx = 10, space = "shifted")
spaceSamples(giottoSpace(g, "shifted"))
spaceSamples(perSampleSpace())
}
