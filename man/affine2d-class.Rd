% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classes-utils.R
\docType{class}
\name{affine2d-class}
\alias{affine2d-class}
\alias{affine2d}
\title{Affine Transform Object}
\description{
Container for accumulating 2D affine transformations. Simple spatial
transforms (\code{\link[=spatShift]{spatShift()}}, \code{\link[=spin]{spin()}}, \code{\link[=rescale]{rescale()}}, \code{\link[=flip]{flip()}}, \code{\link[=t]{t()}},
\code{\link[=shear]{shear()}}) can be chained on an \code{affine2d} to build up a combined
transform, which is then applied to spatial objects in a single step via
\code{\link[=affine]{affine()}}.

Create an identity \code{affine2d} with \code{affine()} (no arguments).
Before chaining centroid-relative operations (\code{spin()},
\code{rescale()}) without explicit \code{x0}/\code{y0}, set
\code{ext(aff) <- ext(your_object)} so the pivot point matches your data.

The combined linear transform is stored as a 3x3 homogeneous matrix in
\code{@affine}. Translations are encoded in column 3 (rows 1-2). The
convention is post-multiply: \code{xy_out = xy_in \%*\% A}.
}
\section{Slots}{

\describe{
\item{\code{anchor}}{numeric(4). Anchoring spatial extent
(\code{xmin, xmax, ymin, ymax}). Set via \code{ext(aff) <-} to match
the data extent. Used as the centroid pivot for \code{\link[=spin]{spin()}} and \code{\link[=rescale]{rescale()}}
when \code{x0}/\code{y0} are not supplied, and for translation
calculation during transform composition.}

\item{\code{affine}}{matrix. 3x3 homogeneous transform matrix encoding the
combined linear + translation transform.}

\item{\code{order}}{character. Records the order in which component operations
were applied.}

\item{\code{rotate}}{numeric(1). Accumulated rotation angle in radians.}

\item{\code{shear}}{numeric(2). Accumulated shear factors \code{(x, y)}.}

\item{\code{scale}}{numeric(2). Accumulated scale factors \code{(x, y)}.}

\item{\code{translate}}{numeric(2). Accumulated translation \code{(x, y)}.}
}}

