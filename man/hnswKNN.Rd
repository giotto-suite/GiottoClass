% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nn_search_hnsw.R
\name{hnswKNN}
\alias{hnswKNN}
\title{Approximate k-nearest neighbors via HNSW}
\usage{
hnswKNN(
  x,
  k,
  distance = c("euclidean", "cosine", "l2", "ip"),
  M = 16L,
  ef_construction = 200L,
  ef = 200L,
  n_threads = NULL,
  n_threads_build = 1L,
  ...
)
}
\arguments{
\item{x}{numeric matrix. Rows are observations (cells), columns are
dimensions (typically PCA coordinates).}

\item{k}{integer. Number of neighbors to return per observation, excluding
the observation itself.}

\item{distance}{character. Metric, one of \code{"euclidean"} (default),
\code{"cosine"}, \code{"l2"} (squared euclidean) or \code{"ip"} (inner product).}

\item{M}{integer. HNSW graph degree (default 16). Higher improves recall
at the cost of memory and build time.}

\item{ef_construction}{integer. Beam width during index construction
(default 200). Higher improves recall at the cost of build time.}

\item{ef}{integer. Beam width during search (default 200). This is the
recall/speed dial: higher \code{ef} searches more of the graph, bringing the
result closer to the exact \code{\link[dbscan:kNN]{dbscan::kNN()}} answer at the cost of query
time. Raised to at least \code{k + 1}. On 158,662 cells at \code{k = 30}, \code{ef = 50}
reproduced 99.225\% of the exact network's undirected edges and \code{ef = 200}
reproduced 99.995\%, for 2.30s against 2.83s.}

\item{n_threads}{integer. Threads for the \strong{search}. Defaults to
\code{\link[GiottoUtils:determine_cores]{GiottoUtils::determine_cores()}}. The search is deterministic at any
thread count, so this can be left parallel.}

\item{n_threads_build}{integer or \code{NULL}. Threads for the index \strong{build},
default \code{1}. A multithreaded build is not reproducible: insertion order
varies, so the graph and hence the neighbours differ slightly between
runs, which propagates to clustering even with a fixed seed. Building on
one thread makes repeated calls bit-identical. Set to \code{NULL} to inherit
\code{n_threads} and trade reproducibility for speed while exploring --
measured at 11.8s against 2.82s on 158,662 cells.}

\item{...}{unused, for signature compatibility with \code{\link[dbscan:kNN]{dbscan::kNN()}}.}
}
\value{
object of class \code{c("kNN", "NN")} with elements \code{id} (integer
matrix, \code{nrow(x)} x \code{k}), \code{dist} (numeric matrix, same shape), \code{k},
\code{sort} and \code{metric}. \code{id} and \code{dist} carry \code{\link[dbscan:kNN]{dbscan::kNN()}}'s dimnames
convention: \code{rownames(x)} on the rows, \verb{"1".."k"} on the columns.
}
\description{
Find the \code{k} nearest neighbors of every row of \code{x} using an HNSW index
(Hierarchical Navigable Small World), returning the same structure as
\code{\link[dbscan:kNN]{dbscan::kNN()}} so the two are interchangeable as the search step of
network construction.

HNSW is \emph{approximate}: recall is high but not guaranteed to be 1.0. Use
\code{\link[dbscan:kNN]{dbscan::kNN()}} when exactness matters, or on small data where the exact
search is both faster and exact.

Repeated calls are reproducible by default. The index build is the only
nondeterministic phase -- concurrent insertion makes the graph depend on
thread interleaving -- so \code{n_threads_build} defaults to \code{1}. Searching is
unaffected and stays parallel.
}
\examples{
\dontrun{
m <- matrix(rnorm(1000 * 20), nrow = 1000)
nn <- hnswKNN(m, k = 30)
str(nn$id)
}
}
